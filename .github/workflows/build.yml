name: Build Website

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Upload Website
        uses: actions/upload-artifact@v4
        with:
          name: website
          path: index.html
